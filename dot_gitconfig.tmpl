{{- if eq .chezmoi.os "windows" }}
{{ template "git/common.gitconfig" . }}

# ============================================================================
# Windows-specific settings (only user info remains)
# ============================================================================
[user]
    name = ChaosYD
    email = 936768863@qq.com

# If you need Windows-specific diff/merge tools, add them here.
# Example:
# [diff]
#     tool = nvim
# [difftool "nvim"]
#     cmd = 'nvim -d "$LOCAL" "$REMOTE"'
{{- else }}
# ============================================================================
# FATAL ERROR: This Git configuration file is intended for Windows only.
# It has been deployed to {{ .chezmoi.os }}. Please correct your deployment.
# ============================================================================
[error-intentionally-invalid
{{- end }}
