{{- if ne .chezmoi.os "windows" }}
{{ template "git/common.gitconfig" . }}

# ============================================================================
# Unix-like specific settings (only user info remains)
# ============================================================================
[user]
    name = qdyangdi
    email = 936768863@qq.com

# If you need Unix-specific diff/merge tools, add them here.
# Example:
# [diff]
#     tool = vimdiff
{{- else }}
# ============================================================================
# FATAL ERROR: This Git configuration file is intended for Unix-like systems only.
# It has been deployed to {{ .chezmoi.os }}. Please correct your deployment.
# ============================================================================
[error-intentionally-invalid
{{- end }}
