" ========== XDG compliant Vim main config ==========
" This file is managed by chezmoi. Rendered as ~/.config/vim/vimrc (Linux/macOS).
" Includes common settings and plugin definitions.
" vim-plug (plug.vim) is deployed directly by chezmoi – no auto‑install needed.

" Include common core (basic settings, directories, shell, clipboard)
{{ template "editors/common/core" . }}

" Plugin directory (XDG data directory)
let s:plugged_dir = expand('~/.local/share/vim/plugged')

" Plugin list (from shared template)
call plug#begin(s:plugged_dir)
{{ template "editors/vim/plugins" . }}
call plug#end()

" Common key mappings
{{ template "editors/common/mappings" . }}

" Additional Unix-specific mappings can be added here if needed.
